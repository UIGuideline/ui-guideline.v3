export enum ButtonSize {
  sm = 'sm',
  base = 'base',
  lg = 'lg',
}

export enum ButtonAppearance {
  contained = 'contained',
  outlined = 'outlined',
  ghost = 'ghost',
}

export enum ButtonVariant {
  primary = 'primary',
  neutral = 'neutral',
  destructive = 'destructive',
}
