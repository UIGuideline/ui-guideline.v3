export enum ItemSize {
  sm = 'sm',
  base = 'base',
  lg = 'lg',
}

/* ------------------------------------------------------------ */

export enum ItemVariant {
  neutral = 'neutral',
  danger = 'danger',
  success = 'success',
}

/* ------------------------------------------------------------ */

export enum ToggleGroupSelection {
  single = 'single',
  multiple = 'multiple',
}
