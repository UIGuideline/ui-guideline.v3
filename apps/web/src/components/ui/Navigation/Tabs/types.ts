export enum TriggerSize {
  xs = 'xs',
  sm = 'sm',
  base = 'base',
}

/* ------------------------------------------------------------ */

export enum TriggerVariant {
  primary = 'primary',
  neutral = 'neutral',
}
