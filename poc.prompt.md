# Tips for LLM Implementation

- Component naming convention: Button (main component set name)
- Layer naming: Use exact names from Layer Hierarchy above
- Property naming: Use PascalCase or exact names from the table
- Default values: Always set sensible defaults (Medium size, Primary variant, Contained appearance)
